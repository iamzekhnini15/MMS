spring.application.name=demo

server.port=3000

spring.datasource.url=jdbc:postgresql://localhost:5432/mms
spring.datasource.username=postgres
spring.datasource.password=HayounZ2002/
spring.jpa.generate-ddl=true

azure.blob.service-endpoint=https://mmsfilestorage01.blob.core.windows.net/
azure.blob.container-name=dev
azure.blob.sas-token=${AZURE_BLOB_SAS_TOKEN}

# Configuration upload de fichiers
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload.dir=uploads

# Mail configuration - disabled by default
app.mail.enabled=false
